# OCBC Lumi Chatbot — Dependencies
# Python 3.11+

# Core LangChain
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.0

# LangGraph for agent orchestration
langgraph>=0.2.0

# RAG — vector search
faiss-cpu>=1.7.0
scikit-learn>=1.3.0

# Environment
python-dotenv>=1.0.0

# FastAPI server (HTTP bridge between Lumi and the web frontend)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Supabase REST calls from write tools
requests>=2.31.0

# Web search + URL fetching tools
httpx>=0.27.0
tavily-python>=0.3.0
langchain-community>=0.3.0

# No other dependencies needed — stdlib covers the rest:
# - sqlite3 (conversation memory)
# - json (user profile, behavioral data)
# - datetime, pathlib, collections, re, random (utilities)
