[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install --break-system-packages -r backend/requirements.txt"]

[start]
cmd = "uvicorn server:app --app-dir backend --host 0.0.0.0 --port $PORT"
